BEGIN
    A := 20;
    A99C := 300;
    B := A;
    IF A99C <= B THEN
        A22 := A + B3
    ELSE
        C := ((2 + A) * c + 2) * B;
    D2C1 := C + 100;
    MC := 1;
    IF	10 >= B	THEN
        WHILE MC < 5 DO
            MC := MC + 1
    ELSE
        IF B = C THEN
            C := (C * A) + 4
        ELSE
            A : = 0
END
